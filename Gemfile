# frozen_string_literal: true

source "https://rubygems.org"

gem 'showoff'
gem 'minitest' # comes with 2.5, but this prevents old versions loading
gem 'rspec'

gem 'pry-byebug'

gem 'pdfkit' # static slides generation
